---
import Layout from "../layouts/Layout.astro";
import { ui } from "../i18n/ui.js";

const locale = (Astro.locals.lang as 'en' | 'fr') ?? 'en';
console.log('Locale in index:', locale);
---

<Layout>
  <div class="flex flex-col h-full items-center justify-center bg-gradient-to-br from-primary to-secondary min-h-screen p-4 sm:p-6 md:p-8">
    <div class="hero-content text-center max-w-full">
      <div class="max-w-xs sm:max-w-md md:max-w-lg lg:max-w-xl xl:max-w-2xl px-4">
        <!-- Icône animée -->
        <div class="flex justify-center mb-6 sm:mb-8 animate-bounce">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 sm:h-20 sm:w-20 md:h-24 md:w-24 lg:h-28 lg:w-28 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01" />
          </svg>
        </div>

        <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-bold text-white mb-4 sm:mb-6 leading-tight">
          {ui[locale].index.title}
        </h1>
        <p class="py-4 sm:py-6 text-sm sm:text-base md:text-lg lg:text-xl text-white/90 leading-relaxed">
          {ui[locale].index.description}
        </p>

        <!-- Badges de fonctionnalités -->
        <div class="flex flex-wrap justify-center gap-2 mb-6 sm:mb-8">
          <div class="badge badge-lg bg-white/20 text-white border-white/30 text-xs sm:text-sm">
            ⚡ Rapide
          </div>
          <div class="badge badge-lg bg-white/20 text-white border-white/30 text-xs sm:text-sm">
            🎨 Créatif
          </div>
          <div class="badge badge-lg bg-white/20 text-white border-white/30 text-xs sm:text-sm">
            🤖 IA Intégrée
          </div>
        </div>

        <!-- Bouton CTA -->
        <a href="/generator" class="btn btn-accent btn-sm sm:btn-md md:btn-lg lg:btn-lg shadow-2xl hover:scale-105 transition-transform duration-300 inline-flex items-center gap-2">
          <span class="text-sm sm:text-base md:text-lg">{ui[locale].index.button}</span>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 sm:h-5 sm:w-5 md:h-6 md:w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </a>

        <!-- Stats ou avantages -->
        <div class="stats stats-vertical sm:stats-horizontal shadow-xl mt-8 sm:mt-10 md:mt-12 bg-white/10 backdrop-blur-sm text-white border-white/20">
          <div class="stat place-items-center">
            <div class="stat-title text-white/80 text-xs sm:text-sm">Simple</div>
            <div class="stat-value text-accent text-xl sm:text-2xl md:text-3xl">100%</div>
            <div class="stat-desc text-white/70 text-xs sm:text-sm">Interface intuitive</div>
          </div>
          
          <div class="stat place-items-center">
            <div class="stat-title text-white/80 text-xs sm:text-sm">Gratuit</div>
            <div class="stat-value text-accent text-xl sm:text-2xl md:text-3xl">∞</div>
            <div class="stat-desc text-white/70 text-xs sm:text-sm">Créations illimitées</div>
          </div>
          
          <div class="stat place-items-center">
            <div class="stat-title text-white/80 text-xs sm:text-sm">Thèmes</div>
            <div class="stat-value text-accent text-xl sm:text-2xl md:text-3xl">30+</div>
            <div class="stat-desc text-white/70 text-xs sm:text-sm">Personnalisation</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>